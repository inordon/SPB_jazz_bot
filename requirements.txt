# Оптимизированные зависимости для минимального размера
# Закреплены точные версии для воспроизводимости

# Основные зависимости
aiogram==3.3.0
asyncpg==0.29.0
python-dotenv==1.0.0

# Заменяем Pillow на более легкую альтернативу для Alpine
# pillow==10.2.0  # Тяжелая (много C зависимостей)
pillow-simd==10.2.0  # Более оптимизированная версия

# Легкие альтернативы
aiofiles==23.2.1
aiohttp==3.9.1

# Опциональные зависимости только для продакшена
# (закомментируйте для dev среды)
psutil==5.9.6  # Для мониторинга, но добавляет ~10MB
uvloop==0.19.0  # Для ускорения, но только на Linux